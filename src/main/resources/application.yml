spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce
    username: root
    password: 2004

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
